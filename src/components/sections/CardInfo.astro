---
interface Props {
  title: string;
  text: string;
  stilos: string;
}

const { title, text, stilos } = Astro.props;
---

<article class={stilos}>
  <slot />
  <h4 class="text-2xl font-bold">{title}</h4>
  <p>{text}</p>
</article>
